---
groups:
  - name: pe_xl_nodes
    config:
      transport: ssh
      ssh:
        host-key-check: false
        user: centos
        run-as: root
        tty: true
    nodes:
      - $MASTER$
      - $PUPPET_DB$
      - $COMPILER_A$
      - $COMPILER_B$
